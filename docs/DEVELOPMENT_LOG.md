# Development Log

## 2026-09-07 — v0.5 商店街の質感と動き

ユーザーの「もっとリアル」に対し，方向を「ゲームらしさを保ち，街・質感・動きをリアルにする」と確認した．

- 商店街の窓枠・庇・看板・店先・街灯・排水溝・植栽・遠景を追加．
- 舗装・壁・布・陶器・金属・水・汚れの質感と，空・反射・柔らかな影・接地陰影を更新．
- 中空の丸い便器と衝突メッシュ，水面の波紋・渦・しぶき．
- 人に近い体の比率と肘・膝の動作，一人称の手元，投擲・清掃・食事・巻き込みの演出．
- わずかな歩行揺れ，走行時の視野変化，着地の沈み込み．距離に応じた合成効果音と消音ボタン．
- HUDを小さくし，食堂の説明が店のメニューと重なる表示を解消．
- 静的キャッシュの解放漏れで終了時に出たObjectDB警告を，ゲーム終了時の明示的なキャッシュ解放で解消した．

検証：既存50項目＋演出16項目，計66項目が成功．180秒のCPU試合を完走．
通常描画でメニュー・街・汚れ・食堂・一人称・得点・水流と小ウィンドウを確認し，走行・跳躍・清掃・投擲の途中も画像確認．
Apple M3 / Metal / 1280×720の暖機後360フレーム測定は中央値8.76ms，95パーセンタイル18.55ms．場面限定の測定．

詳細：[v0.5仕様](../specs/v0_5_city_presentation.md)．外部アセットのダウンロードは不要．
CPU対戦の試作を継続し，対人・オンライン・パッド・BGM，専用モーション素材は未実装．

---

## 2026-09-07 — v0.4 街でのトイレ陣取り（ユーザーの構想に訂正）

### 訂正

v0.3のボールを便器へ押す競技は，ユーザーの想定と違っていた．
ユーザーの5項目（敵便器へのうんこ得点，汚れ・滑り・清掃，自陣食堂で補給，近接視点，街と各陣2基の自動生成）を本線仕様とする．
追加で「敵プレイヤー本人も流せる」を確認した．旧試作は保存する．

### 実装

- `scenes/city_battle.tscn` をメインに変更．`scripts/city/` に新しいゲームを分離．
- 肩越し三人称＋一人称切替，マウス照準，カメラの壁との衝突，着地点・予測軌道表示．
- 放物線投擲と区間レイによる衝突判定．敵便器の水面への下降通過で1点．
- 近距離でも便器の縁を越える最短飛行時間を設定．壁越しの得点を防ぐ．
- 地面への着弾で汚れセルを生成．慣性による滑り，ブラシで汚れを除去し近くの敵も押し出す．
- 汚れの描画は2つのMultiMeshへ集約．同じ場所への重複描画を防ぐ．
- 両陣の食堂で，自チームだけ1.5秒の食事後に6個まで補給．
- 自陣の便器の水流で近くの敵を吸い込み，3秒後に敵陣へ復帰．再使用待ちと復帰保護あり．
- 中央通り・横道・建物・食堂・樹木の街．便器を各チーム2基ずつ安全な広場へ生成．シード再生成に対応．
- プレイヤー＋味方CPU 対 敵CPU2体．CPUは進軍・投擲・食堂補給・清掃・水流を使用．
- 操作説明，所持数，地図，得点・試合時計，食事進捗，復帰，一時停止と結果のUI．

### 検証

- Godot 4.7：`tests/test_city.gd` の **50項目が成功**．
- 実投擲による両側の得点，地面の汚れ，滑りと清掃，自陣限定の補給，敵だけの吸い込み，復帰保護，再生成・再試合を含む．
- 180秒のCPU試合を最後までシミュレーションし，得点・汚れ・終了画面までの進行を確認．人間は無操作として実行．
- 通常のMetal描画で，街・汚れ・食堂・一人称・得点・水流・メニューを画像確認．小ウィンドウも確認．
- 初期描画で見つかった，カメラ直前の看板が巨大化する問題を表示距離で修正．

### 残る範囲

街・キャラクターは仮モデル．AIと数値バランスは暫定．
街モードの対人・オンライン・パッド操作と，効果音・BGMは未実装．

---

## 2026-09-07 — v0.3 ローカル対戦

### 変更

- v0.2.5を継続し，1人練習／2人1対1／3人2対1／4人2対2の人数選択を追加．
- P1・P3があか，P2・P4があお．相手色の便器への得点，90秒・5点先取，引き分けと再試合．
- 3秒のキックオフ，得点演出中の時計停止，全員の初期位置復帰，一時停止とロビー復帰．
- キーボード入力を4人分に分離．接続順のパッド4台にも対応し，接続変更時とフォーカス喪失時は一時停止．
- CharacterBody3D同士の押し出しと反動を追加．走行入力とは別に押された速度を保つ．
- ボールの吸い込みアニメーション，連続衝突判定と速度上限を追加．
- ゴール判定を入口平面の横切りに変更し，背後・側面・上方からの誤得点を防止．
- リセットは最新のロック状態を参照し，古い遅延処理がキックオフ中にボールを解放しないよう修正．
- 対戦用HUDと透視投影カメラ．人数選択・得点・結果を1280×720と960×540で描画確認．
- macOS用 `run-game.command` を追加．Godotアプリの場所を自動検出し，クラス登録をインポートしてから起動する．キャッシュなしの新規コピーでも確認済み．
- README・企画・ゲームデザインを現在の本線にそろえ，v0.3仕様を追加．

### 検証

- Godot 4.7.0（Apple M3 / Metal）でインポート・起動と通常描画を確認．
- `game/tests/test_match.gd`：**54項目成功**．実際の衝突による左右のゴール，両方向からの押し合い，4人の入力分離，ジャンプ，得点中のリセット，タイムアップ，場外復帰，メニューボタンの遷移を含む．
- `game/tests/capture_match.gd`：人数選択・キックオフ・対戦・得点・結果と小ウィンドウの7画面を出力．
- 旧2D試作の全テスト，旧3D水流シーンの起動，`git diff --check` も成功．
- macOSサンドボックス内のGodotはシステム証明書読み込みのエラーを出すが，変更前にも発生．通常起動には発生せず，ゲームのスクリプトエラーはなし．

### 残る確認

- ゲームパッドの実機確認（手元の接続台数は0）と，複数人での面白さ・操作感の評価．
- キーボードの多数同時押しには機種ごとの上限がある．
- 次の追加候補は床の妨害アイテム1種類．AI対戦・効果音・BGMは未実装．

---

## 2026-07-06 — v0.2.5 Toilet Goal Prototype（方針転換）

### 方針転換

『トイレロワイヤル』の本線を「便器内で水流から逃げるゲーム」から、
**Fall Guys 的なわちゃわちゃ感を持つ便器ゴール型の3D物理パーティ競技ゲーム**へ変更。
長方形フィールドでボールを押し合い、左右の巨大便器ゴールに流し込むと水流とともに得点。
v0.2.5 の検証課題は「サッカーのゴールが便器になっている構図が、見た瞬間に面白いか」。

v0.2 は削除せず、水流・警告・吸引・螺旋・カメラ揺れの技術検証として
`scenes/main.tscn` に保存（吸引や水流は今後ゴール演出・場外ギミックとして還流予定）。

### 実装内容（すべて新規シーン・スクリプト。v0.2 のファイルはほぼ未変更）

- `scenes/toilet_goal_prototype.tscn` + `scripts/toilet_goal_game.gd` —
  長方形フィールド（48×28m、壁・センターライン付き）、得点管理、ゴール演出制御、
  ボール場外の自動復帰、R リセット
- `scenes/toilet_goal.tscn` + `scripts/toilet_goal.gd` — 巨大便器ゴール。
  便座リング（縦向き CSGTorus = 開口部）＋タンク＋水面＋渦。開口部の Area3D に
  ボールが入ると得点シグナル。celebrate() で渦回転・水位低下・スプラッシュ。
  チーム名・チームカラーは @export（あか/あお で左右識別）
- `scenes/ball.tscn` + `scripts/ball.gd` — RigidBody3D のボール。
  上面のオレンジキューブで転がりが見える。reset_to_spawn() で中央復帰
- `scenes/goal_player.tscn` + `scripts/goal_player.gd` — ピクト風プレイヤー
  （丸頭＋ずんぐり胴体＋太短い手足＋点目、単色）。走ると手足がバタバタ揺れる。
  接触した RigidBody を押せる（ボールを押す）。入力は p1_* プレフィックス
- `project.godot` — メインシーンを toilet_goal_prototype.tscn に変更
  （v0.2 は `godot --path game res://scenes/main.tscn` で起動可能）
- `scripts/camera_rig.gd` — CAMERA_OFFSET 定数を @export 化（デフォルト値は
  v0.2 と同一なので挙動不変）。v0.2.5 ではフィールド全体が見える固定視点
  （オフセット 0,26,27）として再利用し、ゴール時の kick 揺れも流用
- `scripts/debug_ui.gd` は無変更のまま両シーンで共用

### 将来アイテム（設計メモのみ、specs/v0_2_5_toilet_goal_prototype.md 参照）

うんこ（床の摩擦・障害物系）、ブラシ（スイープ押し出し・掃除）、
トイレットペーパー（固定・トラップ・減速）。ボールは RigidBody、プレイヤーの
移動値は @export なので、外力・摩擦・係数の形で密結合なしに追加できる想定。

### 既知の問題 / 制約

- Godot 未インストール環境で書いたため実行未確認（人間のローカル確認が必須）
- ボールの押し心地（ball_push_accel=30）と便器開口部の通りやすさは実プレイ調整が前提
- 1人用のため得点は「どちらの便器に流し込んだか」の集計のみ（チーム制は v0.3）

### 次にやるべきこと

- 人間の実プレイで「便器がゴールになっている構図」の第一印象を確認
- 良ければ v0.3: ローカル2〜4人対戦（プレイヤー間の押し出し処理が最重要）→ アイテム検証

---

## 2026-07-06 — v0.2 手触り・動画映え改善（ジューシング）

### 目的

v0.3 には進まず、核である「水流に吸い込まれる瞬間が面白いか」を高める小調整。
人間側で Godot 4 での起動確認済み（エディタ保存により project.godot が 4.7 表記に更新、
スクリプトの .uid ファイルが生成されている）。

### 変更内容

- **定数整理**: 吸引系（FlushController）と移動系（Player）の調整値を `@export` 化。
  Godot インスペクタから再起動なしで手触り調整できる
- **吸引**: 立ち上がりランプ追加（開始0.6sで最大へ、突然引っ張られる違和感防止）。
  穴直近の最大係数 1.4 → 1.6（「最後のひと引き」を強調）
- **警告演出**: 進行度に応じてタンク振動・画面揺れ・点滅速度がエスカレート。
  点滅テキストに残り秒数を表示。水面が波打ちながら上昇
- **螺旋演出**: 1.3s → 1.6s に延長、回転は終盤ほど加速。縮みながら縦に伸びる
  タフィー効果。「うわああああ！」の Label3D 吹き出し。水没時にスプラッシュ
- **カメラ**: 吸い込まれ中は自動クローズアップ（zoom 0.6）、脱落の瞬間に単発の揺れ（kick）
- **UI**: フェーズで状態表示を色分け（通常=白、警告=橙、水流=青）。
  R キーのキーリピート対策（is_echo で弾く。前回レビューの Can wait 対応）
- specs/v0_2_3d_graybox.md に手触り調整ガイドを追加

### Codexレビュー（実施済み・静的レビュー）

- Critical / 今すぐ直すべき問題: なし。Tween の "albedo_color:a" パス、Label3D の tscn 記法、
  is_echo() などの Godot 4 API はドキュメントと照合して妥当と確認された
- Minor 4件はすべて対応済み:
  - スペックの古い数値2箇所（距離係数 1.4→1.6、螺旋 1.3s→1.6s）を更新
  - warn_progress() のゼロ除算ガード（インスペクタで warn_time=0 にされた場合）
  - 螺旋の進行度 t のクランプ（巨大 delta 時の見た目破綻防止）
- スコープ超過なし。v0.3 拡張時の注意として main.gd の単一プレイヤー直参照
  （UI・カメラ・スプラッシュ）を配列化する必要がある点が再指摘された

### 次にやるべきこと

- 人間の実プレイで「吸い込まれる瞬間」の主観評価 → OKなら v0.3（ローカル2〜4人対戦）へ

---

## 2026-07-06 — v0.2 3D Graybox Prototype

### 方針転換の明確化

最終的な方向性は平面2Dではなく、「めっちゃカメレオン」系の**3D物理パーティアクション**。
v0.1 の 2D Canvas 版（`prototype/`）は完成版の土台ではなく、
「水流に吸い込まれる遊びが成立するか」を検証した**参考実装**として保存する。

### 目的

「3D空間で、巨大トイレの水流に吸い込まれる挙動が面白いか」の検証。

### 技術選定

- Godot 4（4.3+ 想定、Forward+）。小規模プロトタイプ向き・依存が軽い・
  グレーボックスを素早く作れる・ローカル2〜4人対戦へ拡張しやすい
- この開発環境には Godot 未インストールのため実行確認はしていない。
  人間がローカルで確認する手順を specs/v0_2_3d_graybox.md に記載

### 実装内容（`game/` ディレクトリ、`prototype/` とは独立）

- ステージ = 巨大トイレの上。便座リング（CSG円環）＋タンク（箱）＋中央の便器穴＋水面
- プレイヤー: CharacterBody3D。WASD/矢印移動、Spaceジャンプ。空中は入力が効きにくい
- 水流: idle 6s → warning 2.5s → flushing 3.5s の状態機械（flush_controller.gd、純ロジック）
- 吸引: 穴に近いほど強い加速度、空中は1.8倍。2D版で検証したバランス設計を移植
  （中央付近で無入力なら吸い込まれ、外周なら耐えられ、逃げれば抵抗できる）
- 警告演出: タンク振動・水位上昇・「ゴゴゴゴ…」点滅・カメラ微振動
- 脱落: 便器穴 → 螺旋吸い込み演出 → リスポーン。外周転落 → リスポーン
- カメラ: プレイヤー追従の見下ろし固定角。脱落演出も追うので吸い込みが画面に映る
- デバッグUI: フェーズ・残り時間・回数・イベントログ・操作説明。R でリセット
- 拡張性: 入力は `p1_*` プレフィックス、Player は `player_index` 持ち、
  吸引は問い合わせ式でプレイヤー数非依存 → v0.3 のローカル対戦に対応しやすい構造

### Codexレビュー（実施済み・静的レビュー）

- Must fix 1件: Camera3D の `current` が未設定で意図した視点にならない可能性
  → `main.tscn` に `current = true` を追加して修正済み
- Can wait として指摘（v0.3以降で対応）:
  - R キーのキーリピートで連続リセットされ得る（`is_echo()` で弾くと安定）
  - main.gd / UI / カメラが単一プレイヤー直参照（v0.3 で配列・グループ走査に切替）
  - プレイヤー同士の押し合いは CharacterBody3D だけでは発生しない（v0.3 でカスタム押し出しが必要）
  - 極端に大きい delta でフェーズ遷移が1フレーム1回のみ（実害小）
- 2D版は未変更、スコープ超過なし、不要な大規模変更なしと確認された

### 既知の問題 / 制約

- **未実行**: Godot 未インストール環境で書いたため、人間のローカル確認が必須
- 吸引・ジャンプ等の数値は2D版からの換算で暫定。実プレイ調整が必要
- 効果音・BGMなし（スコープ外）

### 次にやるべきこと

- 人間が Godot 4.3+ で開いて動作確認（specs/v0_2_3d_graybox.md のチェックリスト参照）
- 挙動が面白ければ v0.3: ローカル2〜4人対戦（押し合い・勝ち残り判定）

---

## 2026-07-06 — v0.1 Flush Prototype

### 目的

「巨大トイレの水流に吸い込まれるだけで面白いか」を検証する最小プロトタイプの実装。

### 技術選定

- HTML5 Canvas + 素の JavaScript（依存ゼロ、ビルド不要）
- 理由: 開発環境に Godot が未インストールで実行確認できないため。
  ブラウザで開くだけで動き、ロジックを Node.js でテストできる。
  核が面白いと確認できたら Godot 4 への移植を検討する。

### 実装内容

- `prototype/sim.js` — 物理・水流状態機械（idle 5s → warning 2.5s → flushing 3s のループ）。
  吸引はトイレへの距離で強くなる加速度。地上より空中のほうが強く流される。
  プレイヤーは配列＋スポーン地点4つで、ローカル2〜4人対戦に拡張可能な構造。
- `prototype/game.js` — Canvas 描画（巨大トイレ、渦、風パーティクル、画面振動、
  警告演出「ゴゴゴゴ…」、螺旋吸い込み演出、パニック顔）、キー入力、イベントログ。
- `prototype/index.html` — エントリポイント。
- `prototype/test_sim.js` — Node で動く簡易テスト（25件、全て成功）。
- `specs/v0_1_flush_prototype.md` — 実装スペック。
- `README.md` — 実行方法。

### バランス（テストで担保）

- 中央寄り（x=300）で無入力 → 1回の水流で流される
- 画面端で無入力 → 生き残れる（左右が安全地帯）
- 逃げ方向に入力し続ければ抵抗できる（操作不能ではない）

### Codexレビュー（実施済み）

- Must fix 1件: 脱落判定 `deathY: 500` が水面 `waterY: 524` より上で発火し、
  仕様「水面より下に到達で脱落」とずれる → `deathY: 526` に修正済み（テスト全件成功を再確認）
- Can wait として指摘（v0.2以降で対応）:
  - `game.js` が単一プレイヤー固定（`createWorld(1)`、`KEYMAPS[0]`、UIが `players[0]` 前提）
  - 吸引方向がトイレ中心ぴったりで右向きになる（中心で0が直感的）
  - 当たり判定が中心点のみ（便器のフチで半分はみ出て立てる）
  - R キーのキーリピートでリセットログが連発する
  - 極端に大きい dt でフェーズ遷移が1回分しか処理されない（描画側で dt を 1/30 に制限済みのため実害小）
- スコープ外の実装・不要な大規模変更なしと確認された

### 既知の問題

- 効果音・BGMなし（v0.1 スコープ外）
- 吸引の数値は暫定。実プレイでの調整が必要
- プレイヤーの当たり判定は中心点のみ（便器のフチで半分はみ出て立てる）
- ジャンプはキー押しっぱなしで連続ホップする（仕様未定のため放置）

### 次にやるべきこと

- 人間による実プレイ確認（面白いか・動画映えするか）
- 面白ければ v0.2: ローカル2〜4人対戦（キーマップ追加、勝敗判定、押し合い）
